# Local newcomer browser journey — September 12, 2026

Status: passed locally. This run used a disposable SQLite database, the Vite production build served by FastAPI on loopback, and Brave. It did not call Qwen, alter the deployed site, or use the user's existing local databases.

## Observed path

1. The empty library offered “Start with my own idea” and a separate labeled NVIDIA example.
2. The first stage displayed exactly NVIDIA, Apple and Microsoft plus the tokenized-exposure warning.
3. Apple selection opened a blank idea form. Plain-language position/risk labels were visible, the proposed entry remained user-supplied, and slippage stayed under Advanced.
4. Manual continuation created an editable qualitative assumption with the explicit non-numerical review condition.
5. Saving the draft created version one; confirming created version two and opened evidence without requiring Qwen.
6. Public refresh saved Apple CY2026Q2 SEC company facts and a separately dated public Bitget `rAAPL/USDT` observation. The qualitative assumption correctly remained `INSUFFICIENT_EVIDENCE`; the available filing was not forced into a numerical conclusion about that claim.
7. The decision page displayed the current state and version-two Markdown/JSON exports. A human-authored retain reason created version three.
8. Reloading the deep workspace URL restored Apple, the saved assessment and the public source. Returning to the decision stage updated both export links to version three.
9. The decision timeline showed the version-two confirmation, first assessment and version-three retain event with the saved reason.

## Additional offline evidence

- The full backend suite passed 91 tests and the frontend suite passed 33 tests.
- A disposable copy of the pre-change `data/reviso-v1.sqlite3` opened after additive migration, exposed its existing thesis, and returned the three-instrument catalog. The original database was not modified.
- Markdown/JSON export tests verify saved-state-only assembly, earlier-version boundaries, exact assessment-linked answers, safe attachment names and persistence after a retain decision.
- Mocked model tests verify editable suggestions, citation allowlisting, one bounded repair, duplicate question idempotence, stale-context rejection, injection framing and SQLite restart.

## Remaining gates

- The Qwen suggestion and cited-question UI paths were not called live; use the separately frozen five-case proposal only after a new credit authorization.
- Approximately 390px content was visually exercised in the narrow browser window, but a formal keyboard-only audit, 768px pass, browser matrix and three-person newcomer study remain outstanding.
- Production still runs the earlier NVIDIA release. Deployment and snapshots remain separate approvals.

