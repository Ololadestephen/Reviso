# Decisions and feasibility — 2026-09-10

1. Empty repository, no previous user code. Python 3.12 is installed through uv; Desloppify 1.0 is installed. Node 25 is available. No relevant provider credential environment variables were detected (values were not inspected).
2. Build the requested React/Vite + FastAPI stack locally. Deployment is explicitly subject to approval and a host decision; no external site is registered.
3. Initial candidate NVDAON/USDT (Ondo) was rejected for this SDK slice: the public UTA v3 endpoint returned HTTP 400, trading pair does not exist. The public instrument catalog instead identifies RNVDAUSDT, baseCoin rNVDA, quoteCoin USDT, symbolType stock, status online, isReality yes. First instrument is now rNVDA (Reality), not Ondo. Product source: https://www.bitget.com/academy/what-is-rnvda-nvidia-tokenized-stock-bitget and https://www.bitget.com/campaigns/bitget-rtoken . These identify NVIDIA economic exposure and claimed 1:1 backing; they do not establish direct registered share ownership or guaranteed liquidity. No insurance guarantee is inferred from marketing text. Current redemption, regional eligibility and FX remain research limitations.
4. Official toolkit entry redirects to https://github.com/Bitget-AI/agent_hub . Its TypeScript SDK supplies public market operations; select a narrow read-only bridge after checking the SDK methods. Do not implement private authentication.
5. Browser verification subsequently reached the live S2 handbook from the event's Developer guide button. Track, required materials, X post and judging focus are verified in docs/SUBMISSION.md. September 21 UTC+8 is given without exact time; voting/judging dates conflict within the page. The earlier crawler/root-Markdown error did not mean the browser page was absent. Season 1 requirements were not substituted.
6. The user initially selected Qwen on Groq while the Bitget subsidy was pending. On September 11 the Bitget Qwen 3.8 Max key was granted. The sponsored fixed Responses endpoint is now preferred through server-only `BITGET_QWEN_API_KEY`; Groq remains a fallback. The real key was not placed in chat or code. The first authorized extraction attempt hit the original 25-second read timeout without retry. A Bitget-only 90-second read window was then adopted and one authorized retry succeeded in 49.58 seconds. Manual structured confirmation and deterministic replay remain usable. Narrative-quality evaluation remains a release gate.
7. Local `.env` loading is explicit at the Uvicorn entry point via `--env-file .env`; application-module imports do not silently load secrets. Exported process values override file values. The `.env` file remains ignored by Git and frontend variables remain forbidden.
8. Batch 01 showed that the Bitget proxy returned semantically useful narrative arrays while ignoring the requested object schema. Prompt versions v2 repeat the schema in visible input and allow any decoded JSON root to enter the existing one-repair path. Bitget's output allowance is 5,000 tokens after ambiguous cases exhausted 2,500 entirely in reasoning. A frozen five-case post-fix validation passed every complete application contract with five first-attempt responses. It is validation evidence for those constructed cases, not a general research-accuracy result.
9. Deployment preparation is provider-neutral and local until the user selects a host. The container serves the React SPA and `/api` from one FastAPI origin, runs as a non-root user with a read-only root filesystem, receives secrets only at runtime and keeps SQLite on `/app/data`. Default Compose binds only to loopback. Opt-in `REVISO_PUBLIC_DEMO` requires explicit HTTPS origins, allowed hosts and HTTP Basic authentication; this is a narrow hackathon-demo guard, not multi-user authorization.
10. A September 12 clean-install rehearsal and current-source container build passed. The complete browser lifecycle then used one live Qwen extraction and one live evidence review, public NVIDIA FY2027 Q2 evidence and public Bitget rNVDA data. Human actions alone confirmed, revised and retained the thesis. Server logs expose two successful AI API actions but not internal schema repairs, so request accounting is stated as two user-level actions and two-to-four provider requests rather than an invented exact provider count.
11. AWS Lightsail is selected for deployment because the available AWS credit can cover one persistent virtual server without changing the SQLite repository. The prepared target is one 2 GB Ubuntu instance in `us-east-1`, a static IP, Caddy-managed HTTPS and named Docker volumes for SQLite and TLS state. The local AWS CLI session is expired and its configured login session targets the root identity, so infrastructure creation is deferred until a non-root MFA-protected deployment identity and final hostname are available.
12. The deployment was provisioned through the scoped IAM user `reviso-deployer`, not root. `reviso-prod` uses the 2 GB Lightsail bundle in `us-east-1a`, attached static IP `13.216.59.120`, SSH restricted to the administrator's observed `/32`, and public TCP 80/443. Docker Compose stores SQLite and Caddy state in named volumes. A temporary `sslip.io` hostname first allowed trusted HTTPS verification while the purchased domain lacked registry delegation. Vercel DNS now publishes `revisoagent.xyz` to the static IP; Caddy and the application allowlists were switched to that final hostname, and the temporary hostname is no longer served. One deployed extraction and one deployed review succeeded through sponsored Qwen; public NVIDIA/Bitget refresh, application-container restart persistence and final-domain persisted-state checks also passed. Automatic snapshots remain pending explicit approval of their billed storage.
13. The newcomer release is implemented locally without changing production. The explicit allowlist is NVIDIA `RNVDAUSDT`, Apple `RAAPLUSDT` and Microsoft `RMSFTUSDT`; all three exact public Bitget catalog identities were observed online on September 12. Current Apple/Microsoft evidence uses issuer- and CIK-bound SEC company facts; NVIDIA keeps its newsroom parser and uses the matching SEC feed only as retrieval redundancy when the primary path is unavailable. Historical replay remains NVIDIA-only. Follow-up answers are bound to a saved assessment hash and allowlisted evidence IDs, while exports select only saved data through the requested immutable version. A manual Apple browser journey passed with live public evidence and no Qwen request. Live multi-stock Qwen validation and deployment remain separately gated.
14. The authorized newcomer live-Qwen batch used 6 of its 10-request ceiling. Apple and Microsoft suggestion calls timed out once each without retry; NVIDIA completed an initial response and its single repair but both used non-canonical invalidation text and were rejected without persistence. The Apple cited answer passed every storage/citation check. The Microsoft adversarial answer cited only its SEC source, explicitly abstained on competitive defensibility, survived restart and respected stale-context rejection; its raw FAIL is retained because the harness mistakenly searched the stored question for the requested phantom ID, while the report records the offline PASS adjudication without another call. A subsequent credential-free local browser run found and fixed the hidden blank-slippage blocker, then passed the Apple journey, keyboard focus, native-dialog restoration and 390px/768px overflow checks. Production remains unchanged.
15. The local interface release separates public explanation from protected saved research. `/`, `/example`, `/guide`, `/privacy`, `/terms` and the static assets they need are public GET/HEAD paths. `/app`, legacy `/thesis/*` links, all API routes, saved records, exports and Qwen actions retain HTTP Basic authentication in public-demo mode. The example is static and clearly labelled; it does not read SQLite, fetch market/evidence data or call Qwen. The visual system uses a dark indigo-to-violet hero, a clean white application canvas, local vector brand/company marks, a moving three-company focus, project-specific Reviso screen images and a light single-action closing CTA. Production remains on the earlier authenticated build until deployment is separately approved.
16. On September 13 the user approved the reviewed interface release for Git and production. The release was pushed to `main`, an online SQLite backup was created inside the persistent volume, and the Lightsail application image was rebuilt without changing the Caddy or data volumes. The first deployed visual check exposed that the new root-level image files were still behind Basic Auth. The public asset boundary was corrected with an exact allowlist for the brand, step and three company SVGs plus deployment regression coverage; arbitrary paths and `/app` remain protected. No billed automatic snapshot was enabled and no Qwen request was made during deployment verification.

## Capability matrix

| Capability | Source/interface | Access | Current scope/limitations |
| --- | --- | --- | --- |
| Instrument listing | Bitget UTA catalog + rNVDA product documentation | Public | RNVDAUSDT verified online; beneficial exposure differs from direct shares |
| Ticker and visible book | Official SDK 3.3.0 public market | No key | RNVDAUSDT runtime probe; NVDAONUSDT rejected by endpoint; preserve availability |
| Fundamental evidence | NVIDIA official newsroom; investor relations for curated replay | Public | Latest discovered quarterly earnings release; bounded HTML parsing of reported revenue growth and labeled GAAP margin. Missing/stale/ambiguous data abstains. |
| Crypto news/sentiment | Bitget signal tools | Public | Equity relevance not established; excluded from fundamental conclusions |
| Equity reference price | Not selected | Unknown | Premium comparison unavailable until synchronized compatible reference exists |
| AI extraction/classification | Provider-neutral adapters; sponsored Bitget Qwen 3.8 Max first, Groq fallback | Server-side `BITGET_QWEN_API_KEY`; optional `GROQ_API_KEY` | Batch 01 failures recorded; v2 passed a five-case post-fix validation including extraction, injection resistance and persisted review; independent benchmark still pending |
| Production storage | AWS Lightsail named Docker volume | Private demo | Single-host SQLite persisted across an application-container restart and final-domain cutover; automatic snapshots remain pending approval |

## Quality review limitation

The first automated review attempt failed because the Codex subprocess could not initialize in the sandbox. An approved retry initialized outside it but failed with usage-limit exhaustion. No subjective review score or attestation was fabricated. Review/triage remains pending. Mechanical scans and tests can continue.

## Verified local implementation

The official public SDK 3.3.0 bridge successfully returned rNVDA ticker, visible book and catalog metadata on September 10. A September 12 browser refresh showed 218.45 USDT at source time 08:19:25.560 UTC, retrieved 08:19:27.059 UTC. This is a dated verification observation, not a current quote or price assumption. Two user-authorized Qwen UI actions were performed for the browser rehearsal; no account authorization, publication, commit or push was performed.

The working slice uses human confirmation plus deterministic metric floors. Qwen may propose structured assumptions and annotate supplied passages, but cannot confirm a thesis, fetch arbitrary sources, compute financial outputs or override invalidation. Its two historical NVIDIA documents are curated calibration material. Production persistence and private-demo authentication now have a deployed smoke test; independent evaluation, compatible premium/reference data and final-domain browser verification remain release gates. See the README for reproducible local commands and docs/QUALITY.md for scanner output without implied research validity.

## Public disclosure connector — September 11

The investor-relations quarterly index returned HTTP 403 to the automated request. NVIDIA's own newsroom serves earnings releases successfully, so discovery uses the fixed `https://nvidianews.nvidia.com/news?q=financial%20results` listing. Only earnings-release paths on that exact HTTPS host may be fetched. The newest fiscal quarter present in this bounded listing is selected, with no fallback to older reports if it fails. Coverage is limited to this listing; this is not an exhaustive filings feed.

Live verification retrieved the August 26, 2026 FY2027 Q2 release for the quarter ended July 26, 2026. The parser obtained 75.0% GAAP gross margin and 106% reported revenue growth. This is a dated source observation. The immutable assessment stores excerpts and extraction provenance; the document hash fingerprints the received HTML, which is not retained in full. Retrieval itself uses no LLM; the separate September 12 browser journey did send the preserved excerpt and confirmed assumptions to Qwen for one bounded narrative review.
